/**
 * Summary Table - Shows labeled entity spans with expandable details
 *
 * Split into separate files for maintainability.
 */

export { SummaryTable } from './SummaryTable';
export type { SummaryTableProps, EntitySpan } from './types';

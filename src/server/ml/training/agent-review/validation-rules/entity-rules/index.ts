/**
 * Entity Rules Barrel Export
 *
 * Re-exports all entity validation rules from submodules.
 */

export {
  PUBLISHER_RULES,
  DEMOGRAPHIC_RULES,
  MAGAZINE_RULES,
  RELEASE_DATE_RULES,
  STATUS_RULES,
  FORMAT_RULES,
} from './metadata-rules';

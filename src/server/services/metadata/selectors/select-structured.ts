/**
 * Phase 1.5 commit #8: per-field custom structured selector
 * (rating-JSON merge, externalLinks union, dates earliest-non-null).
 * Stub — body lands in commit #8.
 */

import type { SelectorContext, SelectorResult } from './types';

export function selectStructured(_ctx: SelectorContext): SelectorResult {
  throw new Error('selectStructured: not implemented (Phase 1.5 commit #8)');
}

/**
 * Phase 1.5 commit #6: placeholder-reject + length×language×completeness
 * ranking string selector.
 * Stub — body lands in commit #6.
 */

import type { SelectorContext, SelectorResult } from './types';

export function selectString(_ctx: SelectorContext): SelectorResult {
  throw new Error('selectString: not implemented (Phase 1.5 commit #6)');
}

/**
 * Phase 1.5 commit #5: enum-normalize + majority-vote categorical selector.
 * Stub — body lands in commit #5.
 */

import type { SelectorContext, SelectorResult } from './types';

export function selectCategorical(_ctx: SelectorContext): SelectorResult {
  throw new Error('selectCategorical: not implemented (Phase 1.5 commit #5)');
}

/**
 * FlareSolverr Client
 *
 * Re-exports from the modularized implementation.
 * The client now uses flaresolverr-go (Go binary) instead of Python.
 *
 * @module flaresolverr/flareSolverrClient
 */

export { FlareSolverrClient, DEFAULT_CONFIG, MINIMUM_VERSION } from './flareSolverrClient/index';

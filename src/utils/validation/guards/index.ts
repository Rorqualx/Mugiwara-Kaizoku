/**
 * Type Guards Index
 * 
 * Central export point for domain-specific type guard utilities.
 * Basic type guards should be imported directly from /utils/type-guards
 */

// Export domain-specific guards only
export * from './domain-guards';
export * from './metadata';
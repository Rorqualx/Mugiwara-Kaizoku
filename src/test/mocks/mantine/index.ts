/**
 * Mantine Mocks Index
 * 
 * Central export point for all Mantine-related mocks
 */

// Re-export all mantine mocks
export * from './notifications';
// Note: The .js files (core, hooks, modals) are CommonJS modules
// and are typically imported directly in Jest configuration
// rather than through this index file

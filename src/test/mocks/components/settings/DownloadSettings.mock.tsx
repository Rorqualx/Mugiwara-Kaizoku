/**
 * Mock implementation of DownloadSettings component for build compatibility
 * 
 * This is a simplified mock version of the component for use during build
 * to prevent dependency errors.
 */
import React from 'react';
export function DownloadSettings(): React.ReactElement {
  return <div data-testid="download-settings">Mock DownloadSettings</div>;
}

export default DownloadSettings;

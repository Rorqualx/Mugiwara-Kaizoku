/**
 * Mock implementation of NotificationSettings component for build compatibility
 * 
 * This is a simplified mock version of the component for use during build
 * to prevent dependency errors.
 */
import React from 'react';
export function NotificationSettings(): React.ReactElement {
  return <div data-testid="notification-settings">Mock NotificationSettings</div>;
}

export default NotificationSettings;

/**
 * Mock implementation of EventSettings component for build compatibility
 * 
 * This is a simplified mock version of the component for use during build
 * to prevent dependency errors.
 */
import React from 'react';
export function EventSettings(): React.ReactElement {
  return <div data-testid="event-settings">Mock EventSettings</div>;
}

export default EventSettings;

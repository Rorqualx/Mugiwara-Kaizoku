/**
 * Mock implementation of MetadataProvidersGrid component for build compatibility
 *
 * This is a simplified mock version of the component for use during build
 * to prevent dependency errors.
 */
import React from 'react';

export function MetadataProvidersGrid(): React.ReactElement {
  return <div data-testid="metadata-providers-grid">Mock MetadataProvidersGrid</div>;
}

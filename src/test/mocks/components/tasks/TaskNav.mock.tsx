/**
 * Mock implementation of JobNav component for build compatibility
 * 
 * This is a simplified mock version of the component for use during build
 * to prevent dependency errors.
 */
import React from 'react';
export function JobNav(): React.ReactElement {
  return <div data-testid="task-nav">Mock JobNav</div>;
}
